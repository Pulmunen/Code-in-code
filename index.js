// const testDivContent = document.getElementById('test').innerHTML

// const isSameDomain = (styleSheet) => {
//     if (!styleSheet.href) {
//       return true;
//     }
  
//     return styleSheet.href.indexOf(window.location.origin) === 0;
//   };
  
//   const isStyleRule = (rule) => rule.type === 1;
  
//   const getCSSCustomPropIndex = () =>
//     [...document.styleSheets].filter(isSameDomain)[0].rules[10].style
//     console.log(getCSSCustomPropIndex());

// const styleRules = getCSSCustomPropIndex()

// for (const key in styleRules) {
//     console.log(`${key}: ${styleRules[key]}`);
// }
